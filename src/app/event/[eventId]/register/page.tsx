"use client";

import Layout from "@/app/event/[eventId]/index";

export default function RegisterPage() {
    return (
        <Layout activeTab="register">
            <div>Registration Content</div>
        </Layout>
    );
}
